import { jest } from "@jest/globals";import { createIncidentService } from "../../src/modules/incident/incident.service.js";const notifier={incidentCreated:jest.fn(),statusChanged:jest.fn(),assigned:jest.fn(),escalated:jest.fn()};const repo=()=>({findById:jest.fn().mockResolvedValue({id:"i1",status:"OPEN",assignedToId:null}),findResponder:jest.fn().mockResolvedValue({id:"d1",responderStatus:"AVAILABLE",maxActiveIncidents:5}),countResponderActiveIncidents:jest.fn().mockResolvedValue(0),assign:jest.fn().mockResolvedValue({id:"i1",assignedToId:"d1",assignedToRole:"DISASTER_MANAGER"}),unassign:jest.fn(),addNote:jest.fn().mockResolvedValue({id:"n1",note:"Police contacted"}),createAudit:jest.fn()});describe("Phase 9 coordination",()=>{test("manager self assigns",async()=>{const r=repo(),s=createIncidentService({repository:r,notifier});expect((await s.assign({id:"d1",role:"DISASTER_MANAGER"},"i1")).assignedToId).toBe("d1");});test("manager cannot assign another responder",async()=>{const r=repo(),s=createIncidentService({repository:r,notifier});await expect(s.assign({id:"d1",role:"DISASTER_MANAGER"},"i1","d2")).rejects.toMatchObject({code:"INCIDENT_ASSIGN_OTHER_FORBIDDEN"});});test("staff adds response note",async()=>{const r=repo(),s=createIncidentService({repository:r,notifier});expect((await s.addNote({id:"d1",role:"DISASTER_MANAGER"},"i1","Police contacted")).note).toBe("Police contacted");});});
