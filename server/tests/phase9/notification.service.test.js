import { jest } from "@jest/globals";
import { createNotificationService } from "../../src/modules/notification/notification.service.js";
const repo=()=>({createMany:jest.fn().mockResolvedValue({count:1}),list:jest.fn().mockResolvedValue([]),countUnread:jest.fn().mockResolvedValue(2),findOwned:jest.fn(),markRead:jest.fn(),markAllRead:jest.fn().mockResolvedValue({count:3}),listDisasterManagerIds:jest.fn().mockResolvedValue([{id:"d1"}]),findGroupLeader:jest.fn().mockResolvedValue("leader1")});
describe("Phase 9 notifications",()=>{test("fans out incident notifications",async()=>{const r=repo();const s=createNotificationService({repository:r});await s.incidentCreated({id:"i1",userId:"u1",tripId:"t1",severity:"CRITICAL",title:"SOS",escalationLevel:0});expect(r.createMany.mock.calls[0][0]).toHaveLength(3);});test("protects notification ownership",async()=>{const r=repo();r.findOwned.mockResolvedValue(null);const s=createNotificationService({repository:r});await expect(s.markRead({id:"u1",role:"TOURIST"},"n1")).rejects.toMatchObject({code:"NOTIFICATION_NOT_FOUND"});});});
