import { jest } from "@jest/globals";
import { createSafetyService } from "../../src/modules/safety/safety.service.js";
test("Phase 7 alert is forwarded to Phase 8 incident ingestion",async()=>{const alert={id:"a1",tripId:"t1",userId:"u1",type:"MISSED_CHECK_IN",level:"WARNING",status:"OPEN"};const repository={findDueCheckIns:jest.fn().mockResolvedValue([{id:"c1",tripId:"t1",userId:"u1",dueAt:new Date()}]),markCheckInMissed:jest.fn().mockResolvedValue({}),findOpenAlert:jest.fn().mockResolvedValue(null),createAlert:jest.fn().mockResolvedValue(alert)};const incidentReporter={ingestSafetyAlert:jest.fn().mockResolvedValue({id:"i1"})};const service=createSafetyService({repository,incidentReporter});await service.processDueCheckIns("t1");expect(incidentReporter.ingestSafetyAlert).toHaveBeenCalledWith(alert);});
