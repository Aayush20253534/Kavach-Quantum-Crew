import { ApiResponse } from "../../common/responses/ApiResponse.js";import { escalationService } from "./escalation.service.js";export const createEscalationController=({service=escalationService}={})=>({run:async(_r,s)=>ApiResponse.success(s,{message:"Escalation sweep completed",data:await service.run()})});export const escalationController=createEscalationController();export default escalationController;
