import { ApiResponse } from "../../common/responses/ApiResponse.js";import { notificationService } from "./notification.service.js";
export const createNotificationController=({service=notificationService}={})=>({list:async(r,s)=>ApiResponse.success(s,{data:await service.list(r.user,r.query)}),unreadCount:async(r,s)=>ApiResponse.success(s,{data:{count:await service.unreadCount(r.user)}}),markRead:async(r,s)=>ApiResponse.success(s,{data:await service.markRead(r.user,r.params.notificationId)}),markAllRead:async(r,s)=>ApiResponse.success(s,{data:{updated:(await service.markAllRead(r.user)).count}})});export const notificationController=createNotificationController();export default notificationController;
