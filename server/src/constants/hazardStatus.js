export const HAZARD_STATUSES = Object.freeze({
  PENDING: "PENDING",
  VERIFIED: "VERIFIED",
  REJECTED: "REJECTED",
  RESOLVED: "RESOLVED",
});

export const HAZARD_STATUS_VALUES = Object.freeze(Object.values(HAZARD_STATUSES));
