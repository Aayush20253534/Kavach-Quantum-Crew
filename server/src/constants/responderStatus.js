export const RESPONDER_STATUS = Object.freeze({
  AVAILABLE: "AVAILABLE",
  BUSY: "BUSY",
  OFF_DUTY: "OFF_DUTY",
});

export const RESPONDER_STATUSES = Object.freeze(Object.values(RESPONDER_STATUS));
