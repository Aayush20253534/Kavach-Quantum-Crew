export const HAZARD_SEVERITIES = Object.freeze({
  LOW: "LOW",
  MEDIUM: "MEDIUM",
  HIGH: "HIGH",
  CRITICAL: "CRITICAL",
});

export const HAZARD_SEVERITY_VALUES = Object.freeze(Object.values(HAZARD_SEVERITIES));
